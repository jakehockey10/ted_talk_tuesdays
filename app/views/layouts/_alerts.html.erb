<% flash.each do |message_type, message| %>
    <%= content_tag(:div, message, class: "alert alert-#{message_type} alert-dismissable fade in") do %>
        <button type="button" class="close" data-dismiss="alert">&times</button>
        <%= message %>
    <% end %>
<% end %>